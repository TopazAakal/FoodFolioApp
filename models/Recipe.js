class Recipe {
  constructor(title, ingredients, instructions, image, category, totalTime) {
    this.title = title;
    this.ingredients = ingredients;
    this.instructions = instructions;
    this.image = image;
    this.category = category;
    this.totalTime = totalTime;
  }
}

export default Recipe;
